import { defineConfig } from 'astro/config';

// Deploy config (site/base for GitHub Pages) is deliberately deferred —
// it's a deploy decision, not a design one. We'll set it before first publish.
export default defineConfig({});
