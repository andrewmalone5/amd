<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- 10 Notch — the mountain cut into the tile from below; negative space does the drawing -->
  <defs>
    <mask id="cut">
      <rect width="100" height="100" fill="#fff"/>
      <path d="M22 94 L50 50 L78 94 Z" fill="#000"/>
    </mask>
  </defs>
  <rect x="6" y="6" width="88" height="88" rx="26" fill="#1C1A17" mask="url(#cut)"/>
</svg>
